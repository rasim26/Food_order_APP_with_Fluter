import 'package:flutter/material.dart';

class ListFavourites extends StatelessWidget {
  ListFavourites({super.key});
  List<String> listFavorite = [];

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
